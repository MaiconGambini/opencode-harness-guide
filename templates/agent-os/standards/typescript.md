# TypeScript Standard

- Keep `strict` type safety; avoid `any`.
- Model external data with explicit interfaces or schemas.
- Prefer pure helpers for business transformations.
- Keep framework-specific code at boundaries.
- Use named exports for shared modules.
- Handle loading, empty, and error states explicitly.
