# React/Next Standard

- Prefer Server Components in Next.js; use Client Components only for interactivity.
- Keep server-only secrets outside browser bundles.
- Use typed props and small focused components.
- Separate data loading, formatting, and presentation.
- Include loading, error, and empty states.
- Public pages need metadata, canonical URLs, and structured data when relevant.
