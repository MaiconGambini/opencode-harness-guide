# UI Standard

- Design first for user-facing work.
- Use project tokens for color, spacing, radius, and typography.
- Support keyboard navigation and accessible labels.
- Avoid generic visual choices when product identity matters.
- Motion must clarify state or hierarchy, not decorate randomly.
- Verify responsive behavior on small and desktop screens.
